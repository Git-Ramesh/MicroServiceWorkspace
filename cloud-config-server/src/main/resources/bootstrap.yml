server:
  port: 4014
spring:
  application:
    name: cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: ${HOME}/MicroServiceWorkspace/config-server-repo
#        bootstrap: true
